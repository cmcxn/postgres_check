# 新功能说明文档

## 功能概述

本次更新添加了三个重要的监控和通知功能，以提高系统的可靠性和可观察性。

### 1. 恢复告警（Recovery Alert）

**功能描述**：
当系统从"无数据"告警状态恢复到正常状态时，自动发送恢复通知邮件，并统计故障持续的时间。

**工作原理**：
- 系统维护一个告警状态标志（`inAlertState`）
- 第一次检测到无数据时，发送告警邮件并记录告警开始时间
- 在告警状态期间，不会重复发送告警邮件
- 当数据恢复时，计算故障持续时间并发送恢复邮件
- 系统退出告警状态，恢复正常监控

**配置示例**：
```yaml
app:
  notification:
    recovery-enabled: true  # 启用恢复通知
    recovery-subject: "[恢复] 数据库监控：数据恢复正常"
    recovery-body-template: |
      您好，
      
      数据库监控系统检测到数据已恢复正常。
      
      恢复时间：%s
      监控表名：%s
      故障时长：%s
      故障分钟数：%d 分钟
      
      系统将继续监控数据变化。
      
      此邮件由系统自动发送，请勿回复。
```

**邮件示例**：
```
主题：[恢复] 数据库监控：数据恢复正常

您好，

数据库监控系统检测到数据已恢复正常。

恢复时间：2024-01-15 10:30:00
监控表名：sync_operation_log
故障时长：1 小时 25 分钟
故障分钟数：85 分钟

系统将继续监控数据变化。

此邮件由系统自动发送，请勿回复。
```

**时长格式化**：
- 小于60分钟：显示为 "X 分钟"
- 小于24小时：显示为 "X 小时 Y 分钟"
- 超过24小时：显示为 "X 天 Y 小时"

---

### 2. 每日健康检查（Daily Health Check）

**功能描述**：
每天定时发送一封健康检查邮件，确认监控系统正常运行。

**目的**：
- 证明监控系统仍在运行
- 避免长时间无告警导致误以为系统正常
- 如果长期未收到健康检查邮件，说明监控系统可能已崩溃

**工作原理**：
- 使用Spring的定时任务功能
- 默认每天早上8:00执行
- 执行时间可通过cron表达式自定义
- 发送包含系统状态的邮件

**配置示例**：
```yaml
app:
  notification:
    daily-health-check-enabled: true  # 启用每日健康检查
    daily-health-check-cron: "0 0 8 * * ?"  # 每天早上8:00
    daily-health-check-subject: "[正常] 数据库监控系统运行正常"
    daily-health-check-body-template: |
      您好，
      
      这是每日健康检查报告。数据库监控系统运行正常。
      
      检查时间：%s
      监控表名：%s
      检查频率：每 %d 分钟
      
      系统正在持续监控数据变化，如有异常将及时通知您。
      
      此邮件由系统自动发送，请勿回复。
```

**Cron表达式示例**：
- `0 0 8 * * ?` - 每天早上8:00
- `0 0 9 * * ?` - 每天早上9:00
- `0 0 8,20 * * ?` - 每天早上8:00和晚上20:00（两次）
- `0 0 8 * * MON-FRI` - 每周一到周五早上8:00

**邮件示例**：
```
主题：[正常] 数据库监控系统运行正常

您好，

这是每日健康检查报告。数据库监控系统运行正常。

检查时间：2024-01-15 08:00:00
监控表名：sync_operation_log
检查频率：每 5 分钟

系统正在持续监控数据变化，如有异常将及时通知您。

此邮件由系统自动发送，请勿回复。
```

---

### 3. 关闭通知（Shutdown Notification）

**功能描述**：
当应用程序关闭时（正常关闭或异常退出），发送关闭通知邮件。

**目的**：
- 区分正常关闭和异常崩溃
- 及时发现监控服务中断
- 避免因监控服务停止导致的"沉默期"

**工作原理**：
- 使用Spring的`@PreDestroy`注解
- 在JVM关闭前执行
- 发送关闭通知邮件
- 等待2秒确保邮件发送完成

**配置示例**：
```yaml
app:
  notification:
    shutdown-enabled: true  # 启用关闭通知
    shutdown-subject: "[通知] 数据库监控系统已关闭"
    shutdown-body-template: |
      您好，
      
      数据库监控系统已关闭。
      
      关闭时间：%s
      监控表名：%s
      
      如果这是意外关闭，请立即检查系统状态。
      监控服务已停止，将不再发送告警邮件。
      
      此邮件由系统自动发送，请勿回复。
```

**邮件示例**：
```
主题：[通知] 数据库监控系统已关闭

您好，

数据库监控系统已关闭。

关闭时间：2024-01-15 18:30:00
监控表名：sync_operation_log

如果这是意外关闭，请立即检查系统状态。
监控服务已停止，将不再发送告警邮件。

此邮件由系统自动发送，请勿回复。
```

**注意事项**：
- `kill -9`（强制杀死）可能无法发送关闭通知
- 系统崩溃时可能无法发送通知
- 正常关闭（`kill`或`Ctrl+C`）会发送通知

---

## 完整的监控流程

### 正常运行流程

```
1. 应用启动
   ↓
2. 发送启动通知邮件
   ↓
3. 每5分钟检查数据（正常）
   ↓
4. 每天8:00发送健康检查邮件
   ↓
5. 应用关闭
   ↓
6. 发送关闭通知邮件
```

### 故障和恢复流程

```
1. 数据正常更新
   ↓
2. 数据停止更新（故障发生）
   ↓
3. 5分钟后检测到无数据
   ↓
4. 发送告警邮件（首次）
   ↓
5. 进入告警状态，记录告警时间
   ↓
6. 后续检查仍无数据（不重复发邮件）
   ↓
7. 数据恢复更新
   ↓
8. 下次检查检测到有数据
   ↓
9. 计算故障时长
   ↓
10. 发送恢复通知邮件
   ↓
11. 退出告警状态，恢复正常监控
```

---

## 配置管理

### 禁用某个功能

如果不需要某个功能，可以在配置中禁用：

```yaml
app:
  notification:
    recovery-enabled: false           # 禁用恢复通知
    daily-health-check-enabled: false # 禁用每日健康检查
    shutdown-enabled: false           # 禁用关闭通知
```

### 自定义邮件内容

所有邮件内容都支持自定义，使用占位符：

**恢复通知占位符**：
- `%s` - 恢复时间（第1个）
- `%s` - 表名（第2个）
- `%s` - 故障时长描述（第3个）
- `%d` - 故障分钟数（第4个）

**每日健康检查占位符**：
- `%s` - 检查时间（第1个）
- `%s` - 表名（第2个）
- `%d` - 检查频率分钟数（第3个）

**关闭通知占位符**：
- `%s` - 关闭时间（第1个）
- `%s` - 表名（第2个）

---

## 测试方法

### 测试恢复通知

1. 停止数据同步服务
2. 等待5分钟以上
3. 观察收到告警邮件
4. 启动数据同步服务
5. 等待5分钟以上
6. 观察收到恢复邮件

### 测试每日健康检查

方法1：等待到配置的时间
方法2：临时修改cron表达式测试（如：`0 * * * * ?` 每分钟执行）

### 测试关闭通知

1. 正常关闭应用（`Ctrl+C`或`kill PID`）
2. 观察收到关闭通知邮件

---

## 日志输出

### 告警状态变化

```
# 进入告警状态
警告：sync_operation_log 表在过去 5 分钟内没有新增数据
系统进入告警状态，告警开始时间: 2024-01-15 09:00:00

# 告警状态持续
系统已在告警状态，跳过重复告警邮件

# 恢复正常
正常：sync_operation_log 表在过去 5 分钟内有 10 条新增数据
系统从告警状态恢复，故障持续时间: 85 分钟
恢复通知邮件已发送至: admin@example.com
系统恢复正常状态
```

### 每日健康检查

```
==================== 开始每日健康检查 ====================
每日健康检查邮件已发送至: admin@example.com
==================== 每日健康检查完成 ====================
```

### 关闭通知

```
应用正在关闭，准备发送关闭通知邮件
关闭通知邮件已发送至: admin@example.com
```

---

## 常见问题

### Q1: 为什么没收到恢复通知？

**可能原因**：
1. `recovery-enabled` 设置为 `false`
2. 系统从未进入告警状态（数据一直正常）
3. 邮件配置有问题

### Q2: 每日健康检查没有执行？

**可能原因**：
1. `daily-health-check-enabled` 设置为 `false`
2. Cron表达式配置错误
3. 时区设置不正确

### Q3: 关闭通知没有发送？

**可能原因**：
1. `shutdown-enabled` 设置为 `false`
2. 使用 `kill -9` 强制杀死进程
3. 系统崩溃导致无法执行关闭逻辑
4. 邮件发送超时（已等待2秒）

### Q4: 如何调整健康检查时间？

修改 `daily-health-check-cron` 配置：

```yaml
daily-health-check-cron: "0 0 9 * * ?"  # 改为早上9:00
```

---

## 优势和收益

1. **及时发现问题**：恢复通知让你知道问题已解决，无需持续关注
2. **故障分析**：准确的故障时长有助于分析问题严重程度
3. **系统可靠性**：每日健康检查确认监控系统仍在运行
4. **避免盲区**：关闭通知防止监控服务静默失败
5. **减少告警疲劳**：不会在告警状态持续时重复发送邮件

---

**注意**：所有新功能都向后兼容，不影响现有功能的使用。
